{
  "configurations": [
    {
      "consoleName": "cortex-core",
      "name": "cortex-core",
      "type": "debugpy",
      "request": "launch",
      "module": "uvicorn",
      "cwd": "${workspaceFolder}",
      "args": ["app.main:app"],
      "jinja": true
    },
    {
      "consoleName": "cortex-core (clean each reload)",
      "name": "cortex-core (clean each reload)",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/scripts/launch_clean_debug.py",
      "cwd": "${workspaceFolder}",
      "jinja": true
    }
  ]
}
